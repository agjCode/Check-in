import React, { useEffect, useState } from "react";

import {
    Typography,
    Box,
    CircularProgress,
    useTheme,
} from "@mui/material";

const Home : React.FC = () => {
    const theme = useTheme();
    return(
        <Box 
            sx = {{
            flexGrow: 1,
            }}
        >
            Bruh
        </Box>
    );
};

export default Home;